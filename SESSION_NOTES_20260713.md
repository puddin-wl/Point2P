# 2026-07-13 会话记录

> 本文件是历史快照，不是当前状态入口。后续 V2、中心空洞和 Zernike 结论见 `docs/PROJECT_STATUS.md` 与测试目录日志。

## 当时确认的生产参考

- 参考运行：`rtad_mraf_gs_python/artifacts/20260605-144020_rtad_mraf_gs_truncI0135`；
- 初始相位：`artifacts/phase0_beam6p5mm/phase0.mat`；
- 入射光 1/e² 强度直径：6.5 mm；
- 目标：W50=330 μm、H50=120 μm；
- RTAD 边缘：Δx=15 μm、Δy=8 μm；
- 方法：直接 WGS、`flat_local`、200 轮；
- 权重范围：0.5–1.5；
- `mraf_factor=0.8`，`bg_factor=0.9`。

## 当时的实验问题

实验光斑在 X/Y 两个方向均小于设计值。当时保留两种解释：

1. 光路、振镜或扫描透镜引起的几何畸变；
2. 仿真指标与实验/供应商尺寸定义不一致。

现有记录表明，不存在一个统一阈值能同时复现实验 X/Y 尺寸，因此不能简单归因为等比例缩小。应先处理明显的偏斜、剪切和对准问题，再用同一测量流程复测。

## 尺寸补偿规则

当前 RTAD 代码以强度定义目标，`size90` 表示 90% 强度宽度。对固定升余弦边缘半宽 Δ：

```text
W90 = W50 - 1.18066894 × Δ
W50 = W90 + 1.18066894 × Δ
```

若要求 size90=330×120 μm 且保持 Δx=15 μm、Δy=8 μm，则初始建议是：

- W50≈347.7 μm；
- H50≈129.4 μm。

实际 7 月后续实验继续扩大到 V2 的 W50=375.02 μm、H50=137 μm，因此本节只保留为计算方法，不是最终参数。

## 后续原则

- 第一步只改变 W50/H50，不同时改变边缘形状；
- X/Y 分别标定，避免假设各向同性；
- 明确强度阈值、振幅阈值或能量尺寸；
- 所有补偿结果继续在 `rtad_mraf_gs_python_test_20260605` 中生成，不覆盖通用管线。
