# 稳定基线清单

本目录保存稳定结果的机器可读清单。清单记录源路径、文件大小、SHA256 和保存方式，
用于确认 Git、Git LFS 或外部归档中的文件没有被替换。

- `20260715_v2.json`：当前实验 V2 的纯 WGS 相位、配置和现场加载 BMP；其中用于
  生成 GDS 的原始 `phase_refined.npy` 已通过 Git LFS 永久保存。

基线状态的文字说明见 [`../docs/PROJECT_STATUS.md`](../docs/PROJECT_STATUS.md)。
