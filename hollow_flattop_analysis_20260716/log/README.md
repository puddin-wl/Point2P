# 分析日志

该目录用于保存本次中心空洞分析中的讨论、实验判断、关键仿真结果和复现信息。

> **当前结论（2026-09-12）**：后续硬件排查确认中心空洞来自 PBS 在极高功率
> 下损坏。以下球差/离焦内容是历史排查记录，不再作为实际根因或补偿依据。
> 详见 [`../docs/ROOT_CAUSE_UPDATE_20260912.md`](../docs/ROOT_CAUSE_UPDATE_20260912.md)。

## 日志批次

- `2026-07-17_spherical_defocus_measured_gaussian`
  - 主题：标准 Zernike 球差 + 离焦，以及换用实测 Gaussian 振幅后的验证；
  - 状态：历史批次；当时判断为球差配合离焦，现已被 PBS 损伤根因取代；
  - 主参数：`Z40=-0.10625`、`Z20=-0.25000` RMS waves。

每个批次中的 `records` 保存讨论和判断，`figures` 保存关键图片，`data` 保存
机器可读结果，`scripts` 保存当时使用的脚本快照。
